{
  "name": "sef-pos-demo-backend",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node serverless-offline.js",
    "deploy": "echo 'Deploy via AWS SAM/Serverless - not configured in demo'"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.667.0",
    "@aws-sdk/lib-dynamodb": "^3.667.0"
  },
  "devDependencies": {}
}